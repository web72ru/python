Леонид Маршев:
надо откатиться назад

0. строку в конце кода html = get_html(URL) зарешёть, она из предыдущего, не нужна

1. ты список ссылок получил (от sbor_ssylok_so_vseh_stranic), теперь возьми только одну ссылку из этого списка

2. теперь надо будет тебе получить хтмл по этой ссылке, для этого воспользуйся функцией получения хтмл

3. а после передай полученный хтмл в функцию которую пишешь
(и только после этого возвращайся к функции которую пишешь)

(всё эти 4 шага в конце кода, за пределами функций)

функция sbor_dannyh_tovara подобна функции get_ssylki
1. принимает хтмл
2. преобразует хтмл в дерево (tree) для парса
3. собирает данные искпасом (это часть расширится, так как данных товара много)
4. возвращает собранные данные

функция get_html, которую ты переименовал в polucaem_html
1. принимает ссылку, по которой нужно загрузить хтмл и принимает параметры (если они есть)
2. загружает реквестом хтмл
3. возвращает хтмл

По поводу этого. Да, то, что возвращает requests.get не является собственно текстом хтмл. 
По крайней мере в нём хранится два свойства, которые тебе нужно знать:
1. В свойстве .text хранится хтмл. 
2. В свойстве .status_code хранится код успешности запроса. Если == 200, то значит страница успешно скачалась. Если 404 - то не загрузилась, или не найдена. Про коды ты слышал в целом.

Пока всё.
Пока читай и перечитывай то, что написал.
Ну если что вопросы задавай (только после внимательного прочтения).

По коду: взаимодействие функций (ВФ) не понято. Под ВФ подразумеваю ретурны и аргументы. Где-то агрументы неправильные, где-то суются аргументы, которые вообще не предназначены для функции, где-то требуются 1 корова, а подаётся целое стадо и тд. С ретурнами пока вроде нет ошибок.

Задача - получить данные отдельного товара. Эта задача в целом по всему коду, всё что не относится к этой задаче необходимо временно зарешётить. (это промежуточная задача, пока я вижу попытки получив все ссылки сразу получить все данные на товары, не спеши, пока получи данные только на один товар)

Ссылка для этого у тебя есть - tovar (переименуй в url_1_tovara).
Функция у тебя для этого создана - sbor_dannyh_tovara (переименуй в sbor_dannyh_1_tovara). (создана, но надо обратить внимание на ВФ)
Посему:
Тебе нужно загрузить страницу (получить хтмл по урл на товар)
И передать эту страницу функции которая соберёт нужные данные со страницы.